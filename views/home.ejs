<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini WhatsApp Home</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <style>
      /* Full screen video background */
      body,
      html {
        margin: 0;
        padding: 0;
        height: 100%;
        font-family: "Roboto", sans-serif;
        overflow: hidden;
      }

      .video-bg {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -1;
      }

      /* Overlay container */
      .overlay {
        position: relative;
        z-index: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
        width: 100%;
        color: white;
        text-align: center;
        background: rgba(0, 0, 0, 0.3); /* dark overlay for text visibility */
      }

      h1 {
        font-size: 50px;
        margin-bottom: 20px;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7);
      }

      p {
        font-size: 18px;
        margin-bottom: 30px;
        text-shadow: 1px 1px 6px rgba(0, 0, 0, 0.7);
      }

      a.button {
        background-color: #25d366;
        color: white;
        padding: 15px 25px;
        border-radius: 8px;
        text-decoration: none;
        font-size: 18px;
        transition: background 0.3s;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
      }

      a.button:hover {
        background-color: #128c7e;
      }
    </style>
  </head>
  <body>
    <!-- Video Background -->
    <video class="video-bg" autoplay muted loop playsinline>
      <source src="/videos/bg.mp4" type="video/mp4" />
      Your browser does not support the video tag.
    </video>

    <!-- Overlay Content -->
    <div class="overlay">
      <h1>Welcome to ChitChat</h1>
      <p>Click below to go to your chats and start messaging!</p>
      <a href="/chats" class="button">Go to Chats</a>
    </div>
  </body>
</html>
